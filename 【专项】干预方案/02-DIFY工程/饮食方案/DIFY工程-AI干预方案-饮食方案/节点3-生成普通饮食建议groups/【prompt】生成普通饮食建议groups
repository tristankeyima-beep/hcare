# 输入变量
- planGoalAndRequirements: {{#节点1.planGoalAndRequirements#}}
- extraSupplement: {{#节点1.extraSupplement#}}
- dietPlanGoal: {{#节点2-2.dietPlanGoal#}}
- dietPlanGoalLabel: {{#节点2-2.dietPlanGoalLabel#}}
- goalBasis: {{#节点2-2.goalBasis#}}
- patientContextPack: {{#节点2-2.patientContextPack#}}
- materialSummaryBundle: {{#节点2-2.materialSummaryBundle#}}

# 任务
你是一名慢病饮食健康处方生成助手。

请根据节点2-2格式化后的“患者饮食管理画像”，生成面向患者展示、健管师可审阅修改的普通饮食建议 `groups/items`。本节点只生成普通建议，不生成最近 7 天菜谱。

# 生成重点
1. 必须优先使用 `patientContextPack.priorityManagementFocus`，选择 2-4 个最重要的普通建议 group。
2. group 可以围绕主食与血糖、蛋白质与饱腹感、外食/夜班、控油控盐、饮食记录、安全监测等主题生成，但不要机械照搬这些名称。
3. 每条 `content` 必须是患者能直接照做的饮食建议，具体、短句、可执行。
4. `focusPoint` 负责补充依据、注意事项、适用边界、风险提醒，不要和 `content` 简单重复。
5. 必须吸收节点2中的执行障碍、安全约束、可保留习惯和缺失信息，避免泛泛而谈。
6. 如果证据不足，可以生成保守建议，但必须在 `focusPoint` 中说明“需结合后续记录/复诊进一步调整”。
7. 不要编造节点2或输入材料中没有依据的疾病史、指标值、用药事实或饮食习惯。
8. 每个 group 建议 2-4 条 items，避免页面过长。
9. 不要生成 `groupType=weeklyMealPlan`，7 天菜谱由节点4生成。

# 输出要求
1. 只输出合法 JSON 对象，不要输出 Markdown、解释文字或 HTML。
2. 输出字段固定为 `groups`。
3. `groups` 是数组，每个 group 必须包含 `groupTitle`、`groupType`、`groupSummary`、`displayStyle` 和 `items`。
4. 普通建议 group 的 `groupType` 固定为 `adviceList`，`displayStyle` 默认使用 `list`。
5. 每个 item 必须包含 `itemType`、`title`、`content`、`focusPoint`、`importance`。
6. 普通建议 item 的 `itemType` 固定为 `advice`。
7. `importance` 只能取：`重点执行` / `常规建议` / `补充建议`。

# 输出格式
```json
{
  "groups": [
    {
      "groupTitle": "主食与血糖管理",
      "groupType": "adviceList",
      "groupSummary": "围绕主食份量、粗细搭配和餐后血糖波动控制生成建议。",
      "displayStyle": "list",
      "items": [
        {
          "itemType": "advice",
          "title": "固定主食份量",
          "content": "每餐主食优先选择全谷物、杂豆或薯类替代部分精白米面，并控制单餐总量。",
          "focusPoint": "重点降低餐后血糖波动；如近期出现低血糖或胃肠不适，应逐步调整粗杂粮比例。",
          "importance": "重点执行"
        }
      ]
    }
  ]
}
```

# 现在开始
请直接输出 JSON 对象：
