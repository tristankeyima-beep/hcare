# Agent node SVG icons

16px single-color SVG icons for agent task-step node types. The style follows the provided references: filled paths, `#333333`, compact silhouettes, no gradient, no shadow, no color dependency.

| Node type | File | Visual cue |
| --- | --- | --- |
| start | `start.svg` | Input tracks plus start/play core |
| if-else | `if-else.svg` | Decision diamond with two branch outputs |
| code | `code.svg` | Code chevrons plus slash |
| llm | `llm.svg` | Spark intelligence mark plus connected lower nodes |
| answer | `answer.svg` | Response bubble plus completion check |

